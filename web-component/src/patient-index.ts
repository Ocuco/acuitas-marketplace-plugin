// Import and register the web component
import './patient-widget.ts';
import { PatientWidget } from './patient-widget.ts';

// Export the web component class for module federation
export default PatientWidget;
export { PatientWidget };
