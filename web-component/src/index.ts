// Import and register the web component
import './sample-widget.ts';
import { SampleWidget } from './sample-widget.ts';

// Export the web component class for module federation
export default SampleWidget;
export { SampleWidget };
